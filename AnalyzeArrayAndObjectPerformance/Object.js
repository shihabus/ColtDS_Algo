let instructor = {
    firstName: 'Kelly',
    isInstructor: true,
    favouriteNumbers: [1, 2, 3, 4]
}

let keys = Object.keys(instructor)
let values = Object.values(instructor)
let entries = Object.entries(instructor)
let prop = instructor.hasOwnProperty('firstName')

keys
values
entries
prop