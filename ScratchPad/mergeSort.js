// O(log n)
// Divide and conquer
// Break the given list until each chunk is of a single Element

function MergeSort(list){
    if(list.length>1){
        mid=list/2
        left=list.slice(0,mid)
        right=list.slice(mid)
    }
    left
}